# .backlog/

Local-only backlog for archon. Items live as Markdown files under
`pending/` (open work) and `done/` (completed). `sprints/` holds
sprint planning files. Mirrors of GitHub issues/PRs are kept in
frontmatter only — this directory is not committed (see `.gitignore`).
